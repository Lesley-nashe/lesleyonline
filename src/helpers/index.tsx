export type column = {
  title: String;
  fieled: String;
};

export type checkoutItem = {
  name: String;
  description: String;
  price: number;
  count: number;
};

export type ProductItem = {
    name: String;
    description: String;
    price: number;
    inventoryCount: number;
  };
  
